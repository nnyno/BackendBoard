create table member
(
    id bigint generated by default as identity,
    name varchar(255),
    userid varchar(255),
    password varchar(255),
    primary key (id)
);

create table board
(
    id bigint generated by default as identity,
    userid varchar(255),
    title varchar(255),
    text varchar(255),
    primary key (id)
);